# Tesapi

Set `FORCE_API_KEY` (atau `API_KEY`) dan `SEC_TOKEN` di environment sebelum menjalankan aplikasi.

Contoh `.env`:

```env
FORCE_API_KEY=ganti-dengan-api-key-anda
SEC_TOKEN=ganti-dengan-sec-token-anda
```

Lalu jalankan aplikasi dengan:

```bash
npm start
```